# Copy to `.env` and adjust as needed.
# Only VITE_-prefixed vars are exposed to the client (see Vite docs).

# Filename of the dataset CSV, served from web/public/.
VITE_DATASET_FILE=nearby_stars_merged.csv
